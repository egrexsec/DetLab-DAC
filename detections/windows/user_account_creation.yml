schema_version: 2.0.0
kind: detection
id: DET-0009
title: Windows User Account Creation
description: Detects creation of new local user accounts on Windows systems.
status: testing
severity: medium
author: Mell0wx
domain:
- endpoint
platforms:
- windows
canonical_detection:
  intent: Detects creation of new local user accounts on Windows systems.
  attack:
    primary:
      technique: T1136.001
      tactic: persistence
    related: []
  logsource:
    product: windows
    service: security
  analytics:
    logic:
      selection:
        EventID: 4720
      condition: selection
    selectors:
    - canonical_field: event.code
      platform_field: EventID
      operator: equals
      values:
      - 4720
    condition: selection
telemetry_requirements:
  primary_logsource:
    product: windows
    service: security
  data_sources: []
  cloud_telemetry: []
mappings:
- mapping_id: sigma-core
  platform: sigma
  language: sigma
  status: available
  purpose: canonical
  query:
    logsource:
      product: windows
      service: security
    detection:
      selection:
        EventID: 4720
      condition: selection
  field_mappings:
  - canonical_field: event.code
    platform_field: EventID
    operator: equals
  notes: Current canonical selector logic preserved as the Sigma-style source mapping.
workflow_guidance:
  triage_steps: []
  investigation_steps: []
  escalation_guidance: []
  hunt_suggestions: []
supporting_artifacts:
  artifacts: []
  velociraptor_artifacts: []
related_detections: []
response_actions: []
validation:
  tests:
  - name: Atomic Red Team Create Local User
    source: atomic-red-team
    test_id: T1136.001
  falsepositives:
  - Legitimate IT administration
references:
- https://attack.mitre.org/techniques/T1136/001/
