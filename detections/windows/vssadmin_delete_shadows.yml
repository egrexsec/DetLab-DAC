schema_version: 2.0.0
kind: detection
id: DET-0108
title: Volume Shadow Copy Deletion
description: Detects vssadmin shadow copy deletion commands associated with impact operations and recovery inhibition.
status: testing
severity: critical
author: Mell0wx
domain:
- endpoint
platforms:
- windows
canonical_detection:
  intent: Detects vssadmin shadow copy deletion commands associated with impact operations and recovery inhibition.
  attack:
    primary:
      technique: T1490
      tactic: impact
    related: []
  logsource:
    product: windows
    service: process_creation
  analytics:
    logic:
      selection:
        Image|endswith: \vssadmin.exe
        CommandLine|contains:
        - delete
        - shadows
      condition: selection
    selectors:
    - canonical_field: process.name
      platform_field: Image
      operator: endswith_any
      values:
      - \vssadmin.exe
    - canonical_field: process.command_line
      platform_field: CommandLine
      operator: contains_any
      values:
      - delete
      - shadows
    condition: selection
telemetry_requirements:
  primary_logsource:
    product: windows
    service: process_creation
  data_sources: []
  cloud_telemetry: []
mappings:
- mapping_id: sigma-core
  platform: sigma
  language: sigma
  status: available
  purpose: canonical
  query:
    logsource:
      product: windows
      service: process_creation
    detection:
      selection:
        Image|endswith: \vssadmin.exe
        CommandLine|contains:
        - delete
        - shadows
      condition: selection
  field_mappings:
  - canonical_field: process.name
    platform_field: Image
    operator: endswith_any
  - canonical_field: process.command_line
    platform_field: CommandLine
    operator: contains_any
  notes: Current canonical selector logic preserved as the Sigma-style source mapping.
workflow_guidance:
  triage_steps: []
  investigation_steps: []
  escalation_guidance: []
  hunt_suggestions: []
supporting_artifacts:
  artifacts: []
  velociraptor_artifacts: []
related_detections: []
response_actions: []
validation:
  tests:
  - name: Delete volume shadow copies
    source: attack-simulation
    test_id: T1490
  falsepositives:
  - Rare sanctioned backup maintenance or storage cleanup activity
references:
- https://attack.mitre.org/techniques/T1490/
