schema_version: 2.0.0
kind: detection
id: DET-0104
title: System Information Discovery Commands
description: Detects systeminfo execution used to enumerate host details during ATT&CK system information discovery.
status: testing
severity: medium
author: Mell0wx
domain:
- endpoint
platforms:
- windows
canonical_detection:
  intent: Detects systeminfo execution used to enumerate host details during ATT&CK system information discovery.
  attack:
    primary:
      technique: T1082
      tactic: discovery
    related: []
  logsource:
    product: windows
    service: process_creation
  analytics:
    logic:
      selection:
        Image|endswith: \systeminfo.exe
      condition: selection
    selectors:
    - canonical_field: process.name
      platform_field: Image
      operator: endswith_any
      values:
      - \systeminfo.exe
    condition: selection
telemetry_requirements:
  primary_logsource:
    product: windows
    service: process_creation
  data_sources: []
  cloud_telemetry: []
mappings:
- mapping_id: sigma-core
  platform: sigma
  language: sigma
  status: available
  purpose: canonical
  query:
    logsource:
      product: windows
      service: process_creation
    detection:
      selection:
        Image|endswith: \systeminfo.exe
      condition: selection
  field_mappings:
  - canonical_field: process.name
    platform_field: Image
    operator: endswith_any
  notes: Current canonical selector logic preserved as the Sigma-style source mapping.
workflow_guidance:
  triage_steps: []
  investigation_steps: []
  escalation_guidance: []
  hunt_suggestions: []
supporting_artifacts:
  artifacts: []
  velociraptor_artifacts: []
related_detections: []
response_actions: []
validation:
  tests:
  - name: systeminfo discovery command
    source: attack-simulation
    test_id: T1082
  falsepositives:
  - Administrative inventory or troubleshooting scripts
references:
- https://attack.mitre.org/techniques/T1082/
