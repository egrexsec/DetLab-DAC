version: '3.9'

services:
  api:
    build: .
    command: uvicorn detlab.api:app --host 0.0.0.0 --port 8000
    ports:
      - '8000:8000'

  web:
    build: ./web
    ports:
      - '3000:3000'
    depends_on:
      - api
